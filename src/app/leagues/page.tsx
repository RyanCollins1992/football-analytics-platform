import { PlaceholderPage } from "@/components/placeholder-page";

export default function LeaguesPage() {
  return (
    <PlaceholderPage
      title="Leagues"
      phase="Phase 4 (competition/team/match pages)"
      description="League tables, standings history, and per-competition navigation for every league in src/lib/config.ts will live here."
    />
  );
}
