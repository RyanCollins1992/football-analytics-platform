import { PlaceholderPage } from "@/components/placeholder-page";

export default function AdminPage() {
  return (
    <PlaceholderPage
      title="Admin"
      phase="Phase 3 (provider abstraction) and Phase 4+ (sync jobs)"
      description="API status, database status, last sync time, record counts, and manual sync controls (competitions/teams/fixtures/results/standings/statistics/players/injuries) will live here."
    />
  );
}
